4️⃣ 系统设置优化

  设置 → 系统设置:
  - 修改网站名称
  - 设置网站公告
  - 配置额度显示方式
  - 设置注册选项

  设置 → 运营设置:
  - 配置充值选项
  - 设置定价策略
  - 配置支付方式 (Stripe/易支付)

  ---
  🎨 自定义 UI (可选)

  修改 Logo:

  # 准备好您的 logo.png (推荐 512x512 或更大)

  # 从本地上传到服务器
  scp your-logo.png root@your_server_ip:/opt/new-api/web/public/logo.png

  # 在服务器上重新构建
  cd /opt/new-api
  docker-compose down
  docker-compose up -d --build

  修改网站标题:

  cd /opt/new-api
  nano web/index.html

  # 找到 <title> 标签修改
  # 保存后重新构建
  docker-compose down
  docker-compose up -d --build

  前端文件位置:

  - Logo: /opt/new-api/web/public/logo.png
  - 网站图标: /opt/new-api/web/public/favicon.ico
  - 页面组件: /opt/new-api/web/src/components/
  - 页面路由: /opt/new-api/web/src/pages/

  ---
  💾 数据备份 (重要!)

  设置自动备份:

  # 创建备份脚本
  cat > /opt/backup-newapi.sh << 'EOF'
  #!/bin/bash
  BACKUP_DIR="/opt/backups/new-api"
  DATE=$(date +%Y%m%d_%H%M%S)
  mkdir -p $BACKUP_DIR

  # 备份数据库
  docker exec postgres pg_dump -U newapi_user new_api | gzip > $BACKUP_DIR/db_$DATE.sql.gz

  # 备份应用数据
  tar -czf $BACKUP_DIR/data_$DATE.tar.gz -C /opt/new-api data logs

  # 删除30天前的备份
  find $BACKUP_DIR -name "*.gz" -mtime +30 -delete

  echo "备份完成: $DATE"
  EOF

  # 添加执行权限
  chmod +x /opt/backup-newapi.sh

  # 测试备份
  /opt/backup-newapi.sh

  # 设置每天凌晨2点自动备份
  (crontab -l 2>/dev/null; echo "0 2 * * * /opt/backup-newapi.sh >> /var/log/newapi-backup.log 2>&1") | crontab -

  ---
  🔧 常用管理命令

  # 查看日志
  cd /opt/new-api
  docker-compose logs -f new-api

  # 查看所有容器状态
  docker-compose ps

  # 重启服务
  docker-compose restart

  # 停止服务
  docker-compose down

  # 更新代码并重新构建
  cd /opt/new-api
  git pull
  docker-compose up -d --build

  # 查看资源使用
  docker stats

  ---
  📊 项目信息

  部署配置:
  - 服务端口: 3000
  - 数据库: PostgreSQL (newapi_user / new_api)
  - 缓存: Redis
  - 部署方式: Docker Compose (源码构建)
  - 数据目录: /opt/new-api/data/
  - 日志目录: /opt/new-api/logs/
  - 数据库卷: new-api_pg_data

  默认账号:
  - 用户名: root
  - 密码: 123456 ⚠️ 请立即修改!

  ---
  🎓 下一步学习

  1. 查看官方文档: https://docs.newapi.pro
  2. 了解 API 调用方式: 兼容 OpenAI API 格式
  3. 配置模型映射: 自定义模型名称和定价
  4. 设置用户管理: 创建子账户和令牌

  ---
  🆘 遇到问题?

  如果有任何问题:
  - 📋 查看日志: docker-compose logs -f
  - 🔍 检查容器: docker ps
  - 💬 随时问我!

  ---